{
  "name": "critters-grand-merge-leaderboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute critters-leaderboard --local --file=./schema.sql",
    "db:migrate": "wrangler d1 execute critters-leaderboard --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
